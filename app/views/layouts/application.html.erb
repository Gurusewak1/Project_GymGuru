<!DOCTYPE html>
<html>
<head>
  <title>GymGuru</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <script src="https://js.stripe.com/v3/"></script>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <style>
    body {
      font-family: 'Helvetica Neue', Arial, sans-serif;
      margin: 0;
      padding: 20px;
    }

    nav {
      background-color: rgba(255, 255, 255, 0.9); /* White background with slight opacity */
      color: black;
      padding: 15px 20px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
      border-bottom: 2px solid rgba(224, 224, 224, 0.9); /* Light gray border with slight opacity */
    }

    nav ul {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      justify-content: space-between;
      align-items: center;
    }

    nav li {
      margin: 0 20px;
    }

    nav a {
      color: black;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.3s ease, background-color 0.3s ease;
      padding: 8px 16px;
      border-radius: 4px;
      border: 2px solid transparent;
    }

    nav a:hover {
      color: #ff0000; /* Red color on hover */
      border-color: #ff0000; /* Red border on hover */
      background-color: rgba(255, 0, 0, 0.2); /* Light red background on hover */
    }

    nav a.login-register:hover {
      color: #808080; /* Grey color on hover */
      border-color: #808080; /* Grey border on hover */
      background-color: rgba(128, 128, 128, 0.2); /* Light grey background on hover */
    }

    .secondary-menu {
      background-color: rgba(0, 0, 0, 0.8); /* Black background with 80% opacity */
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.3);
      margin-top: 20px;
      opacity: 1; /* Make it visible by default */
      transform: translateY(0); /* No vertical offset */
      transition: opacity 0.5s ease, transform 0.5s ease;
    }

    .secondary-menu ul {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      justify-content: center;
    }

    .secondary-menu li {
      margin: 0 20px;
      border: 2px solid rgba(255, 255, 255, 0.8); /* White border with 80% opacity */
      border-radius: 4px;
      font-weight: bold;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      padding: 10px 20px;
    }

    .secondary-menu li:hover {
      background-color: rgba(255, 255, 255, 0.1); /* Slight white hover effect */
      box-shadow: 0 4px 8px rgba(255, 255, 255, 0.2);
      transform: translateY(-3px);
    }

    .secondary-menu a {
      color: white;
      font-weight: 900;
      text-decoration: none;
    }

    .secondary-menu a:hover {
      text-decoration: underline;
    }

    .flash {
      padding: 10px;
      margin: 10px 0;
      border-radius: 4px;
      font-weight: bold;
    }

    .flash.notice {
      background-color: #d4edda;
      color: #155724;
      border: 1px solid #c3e6cb;
    }

    .flash.alert {
      background-color: #f8d7da;
      color: #721c24;
      border: 1px solid #f5c6cb;
    }

    /* Responsive Styles */
    @media (max-width: 768px) {
      nav ul,
      .secondary-menu ul {
        flex-direction: column;
        align-items: center;
      }

      nav li,
      .secondary-menu li {
        margin: 10px 0;
      }
    }

    /* Hide menus on login and register pages */
    .hide-menu {
      display: none;
    }
  </style>
</head>

<body>
  

  <% if flash[:notice] %>
    <div class="flash notice">
      <%= flash[:notice] %>
    </div>
  <% end %>

  <%= yield %>

</body>
</html>
